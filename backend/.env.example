# NexusGuard AI - Environment Configuration (Optional)
# The application runs with 100% functionality without any API keys.
# (Uses deterministic grounded template reports as default for GenAI investigation)

# Optional: Set your OpenAI API key to enable live LLM generation for investigator reports
# OPENAI_API_KEY=sk-...
